// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>Defines the schedule for a scheduled report, including the cron expression, time zone, active period, and the schedule state.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct ScheduleConfig {
    /// <p>The schedule expression that specifies when to trigger the scheduled report run. This value must be a cron expression consisting of six fields separated by white spaces: <code>cron(minutes hours day_of_month month day_of_week year)</code>.</p>
    pub schedule_expression: ::std::option::Option<::std::string::String>,
    /// <p>The time zone for the schedule expression, for example, <code>UTC</code>.</p>
    pub schedule_expression_time_zone: ::std::option::Option<::std::string::String>,
    /// <p>The time period during which the schedule is active.</p>
    pub schedule_period: ::std::option::Option<crate::types::SchedulePeriod>,
    /// <p>The state of the schedule. <code>ENABLED</code> means the scheduled report runs according to its schedule expression. <code>DISABLED</code> means the scheduled report is paused and will not run until re-enabled.</p>
    pub state: ::std::option::Option<crate::types::ScheduleState>,
}
impl ScheduleConfig {
    /// <p>The schedule expression that specifies when to trigger the scheduled report run. This value must be a cron expression consisting of six fields separated by white spaces: <code>cron(minutes hours day_of_month month day_of_week year)</code>.</p>
    pub fn schedule_expression(&self) -> ::std::option::Option<&str> {
        self.schedule_expression.as_deref()
    }
    /// <p>The time zone for the schedule expression, for example, <code>UTC</code>.</p>
    pub fn schedule_expression_time_zone(&self) -> ::std::option::Option<&str> {
        self.schedule_expression_time_zone.as_deref()
    }
    /// <p>The time period during which the schedule is active.</p>
    pub fn schedule_period(&self) -> ::std::option::Option<&crate::types::SchedulePeriod> {
        self.schedule_period.as_ref()
    }
    /// <p>The state of the schedule. <code>ENABLED</code> means the scheduled report runs according to its schedule expression. <code>DISABLED</code> means the scheduled report is paused and will not run until re-enabled.</p>
    pub fn state(&self) -> ::std::option::Option<&crate::types::ScheduleState> {
        self.state.as_ref()
    }
}
impl ScheduleConfig {
    /// Creates a new builder-style object to manufacture [`ScheduleConfig`](crate::types::ScheduleConfig).
    pub fn builder() -> crate::types::builders::ScheduleConfigBuilder {
        crate::types::builders::ScheduleConfigBuilder::default()
    }
}

/// A builder for [`ScheduleConfig`](crate::types::ScheduleConfig).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct ScheduleConfigBuilder {
    pub(crate) schedule_expression: ::std::option::Option<::std::string::String>,
    pub(crate) schedule_expression_time_zone: ::std::option::Option<::std::string::String>,
    pub(crate) schedule_period: ::std::option::Option<crate::types::SchedulePeriod>,
    pub(crate) state: ::std::option::Option<crate::types::ScheduleState>,
}
impl ScheduleConfigBuilder {
    /// <p>The schedule expression that specifies when to trigger the scheduled report run. This value must be a cron expression consisting of six fields separated by white spaces: <code>cron(minutes hours day_of_month month day_of_week year)</code>.</p>
    pub fn schedule_expression(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.schedule_expression = ::std::option::Option::Some(input.into());
        self
    }
    /// <p>The schedule expression that specifies when to trigger the scheduled report run. This value must be a cron expression consisting of six fields separated by white spaces: <code>cron(minutes hours day_of_month month day_of_week year)</code>.</p>
    pub fn set_schedule_expression(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.schedule_expression = input;
        self
    }
    /// <p>The schedule expression that specifies when to trigger the scheduled report run. This value must be a cron expression consisting of six fields separated by white spaces: <code>cron(minutes hours day_of_month month day_of_week year)</code>.</p>
    pub fn get_schedule_expression(&self) -> &::std::option::Option<::std::string::String> {
        &self.schedule_expression
    }
    /// <p>The time zone for the schedule expression, for example, <code>UTC</code>.</p>
    pub fn schedule_expression_time_zone(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.schedule_expression_time_zone = ::std::option::Option::Some(input.into());
        self
    }
    /// <p>The time zone for the schedule expression, for example, <code>UTC</code>.</p>
    pub fn set_schedule_expression_time_zone(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.schedule_expression_time_zone = input;
        self
    }
    /// <p>The time zone for the schedule expression, for example, <code>UTC</code>.</p>
    pub fn get_schedule_expression_time_zone(&self) -> &::std::option::Option<::std::string::String> {
        &self.schedule_expression_time_zone
    }
    /// <p>The time period during which the schedule is active.</p>
    pub fn schedule_period(mut self, input: crate::types::SchedulePeriod) -> Self {
        self.schedule_period = ::std::option::Option::Some(input);
        self
    }
    /// <p>The time period during which the schedule is active.</p>
    pub fn set_schedule_period(mut self, input: ::std::option::Option<crate::types::SchedulePeriod>) -> Self {
        self.schedule_period = input;
        self
    }
    /// <p>The time period during which the schedule is active.</p>
    pub fn get_schedule_period(&self) -> &::std::option::Option<crate::types::SchedulePeriod> {
        &self.schedule_period
    }
    /// <p>The state of the schedule. <code>ENABLED</code> means the scheduled report runs according to its schedule expression. <code>DISABLED</code> means the scheduled report is paused and will not run until re-enabled.</p>
    pub fn state(mut self, input: crate::types::ScheduleState) -> Self {
        self.state = ::std::option::Option::Some(input);
        self
    }
    /// <p>The state of the schedule. <code>ENABLED</code> means the scheduled report runs according to its schedule expression. <code>DISABLED</code> means the scheduled report is paused and will not run until re-enabled.</p>
    pub fn set_state(mut self, input: ::std::option::Option<crate::types::ScheduleState>) -> Self {
        self.state = input;
        self
    }
    /// <p>The state of the schedule. <code>ENABLED</code> means the scheduled report runs according to its schedule expression. <code>DISABLED</code> means the scheduled report is paused and will not run until re-enabled.</p>
    pub fn get_state(&self) -> &::std::option::Option<crate::types::ScheduleState> {
        &self.state
    }
    /// Consumes the builder and constructs a [`ScheduleConfig`](crate::types::ScheduleConfig).
    pub fn build(self) -> crate::types::ScheduleConfig {
        crate::types::ScheduleConfig {
            schedule_expression: self.schedule_expression,
            schedule_expression_time_zone: self.schedule_expression_time_zone,
            schedule_period: self.schedule_period,
            state: self.state,
        }
    }
}
