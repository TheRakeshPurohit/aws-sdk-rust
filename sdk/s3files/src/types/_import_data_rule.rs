// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>Specifies a rule that controls how data is imported from S3 into the file system.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct ImportDataRule {
    /// <p>The S3 key prefix that scopes this import rule. Only objects with keys beginning with this prefix are subject to the rule.</p>
    pub prefix: ::std::string::String,
    /// <p>The event that triggers data import. Valid values are <code>ON_DIRECTORY_FIRST_ACCESS</code> (import when a directory is first accessed) and <code>ON_FILE_ACCESS</code> (import when a file is accessed).</p>
    pub trigger: crate::types::ImportTrigger,
    /// <p>The upper size limit in bytes for this import rule. Only objects with a size strictly less than this value will have data imported into the file system.</p>
    pub size_less_than: i64,
}
impl ImportDataRule {
    /// <p>The S3 key prefix that scopes this import rule. Only objects with keys beginning with this prefix are subject to the rule.</p>
    pub fn prefix(&self) -> &str {
        use std::ops::Deref;
        self.prefix.deref()
    }
    /// <p>The event that triggers data import. Valid values are <code>ON_DIRECTORY_FIRST_ACCESS</code> (import when a directory is first accessed) and <code>ON_FILE_ACCESS</code> (import when a file is accessed).</p>
    pub fn trigger(&self) -> &crate::types::ImportTrigger {
        &self.trigger
    }
    /// <p>The upper size limit in bytes for this import rule. Only objects with a size strictly less than this value will have data imported into the file system.</p>
    pub fn size_less_than(&self) -> i64 {
        self.size_less_than
    }
}
impl ImportDataRule {
    /// Creates a new builder-style object to manufacture [`ImportDataRule`](crate::types::ImportDataRule).
    pub fn builder() -> crate::types::builders::ImportDataRuleBuilder {
        crate::types::builders::ImportDataRuleBuilder::default()
    }
}

/// A builder for [`ImportDataRule`](crate::types::ImportDataRule).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct ImportDataRuleBuilder {
    pub(crate) prefix: ::std::option::Option<::std::string::String>,
    pub(crate) trigger: ::std::option::Option<crate::types::ImportTrigger>,
    pub(crate) size_less_than: ::std::option::Option<i64>,
}
impl ImportDataRuleBuilder {
    /// <p>The S3 key prefix that scopes this import rule. Only objects with keys beginning with this prefix are subject to the rule.</p>
    /// This field is required.
    pub fn prefix(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.prefix = ::std::option::Option::Some(input.into());
        self
    }
    /// <p>The S3 key prefix that scopes this import rule. Only objects with keys beginning with this prefix are subject to the rule.</p>
    pub fn set_prefix(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.prefix = input;
        self
    }
    /// <p>The S3 key prefix that scopes this import rule. Only objects with keys beginning with this prefix are subject to the rule.</p>
    pub fn get_prefix(&self) -> &::std::option::Option<::std::string::String> {
        &self.prefix
    }
    /// <p>The event that triggers data import. Valid values are <code>ON_DIRECTORY_FIRST_ACCESS</code> (import when a directory is first accessed) and <code>ON_FILE_ACCESS</code> (import when a file is accessed).</p>
    /// This field is required.
    pub fn trigger(mut self, input: crate::types::ImportTrigger) -> Self {
        self.trigger = ::std::option::Option::Some(input);
        self
    }
    /// <p>The event that triggers data import. Valid values are <code>ON_DIRECTORY_FIRST_ACCESS</code> (import when a directory is first accessed) and <code>ON_FILE_ACCESS</code> (import when a file is accessed).</p>
    pub fn set_trigger(mut self, input: ::std::option::Option<crate::types::ImportTrigger>) -> Self {
        self.trigger = input;
        self
    }
    /// <p>The event that triggers data import. Valid values are <code>ON_DIRECTORY_FIRST_ACCESS</code> (import when a directory is first accessed) and <code>ON_FILE_ACCESS</code> (import when a file is accessed).</p>
    pub fn get_trigger(&self) -> &::std::option::Option<crate::types::ImportTrigger> {
        &self.trigger
    }
    /// <p>The upper size limit in bytes for this import rule. Only objects with a size strictly less than this value will have data imported into the file system.</p>
    /// This field is required.
    pub fn size_less_than(mut self, input: i64) -> Self {
        self.size_less_than = ::std::option::Option::Some(input);
        self
    }
    /// <p>The upper size limit in bytes for this import rule. Only objects with a size strictly less than this value will have data imported into the file system.</p>
    pub fn set_size_less_than(mut self, input: ::std::option::Option<i64>) -> Self {
        self.size_less_than = input;
        self
    }
    /// <p>The upper size limit in bytes for this import rule. Only objects with a size strictly less than this value will have data imported into the file system.</p>
    pub fn get_size_less_than(&self) -> &::std::option::Option<i64> {
        &self.size_less_than
    }
    /// Consumes the builder and constructs a [`ImportDataRule`](crate::types::ImportDataRule).
    /// This method will fail if any of the following fields are not set:
    /// - [`prefix`](crate::types::builders::ImportDataRuleBuilder::prefix)
    /// - [`trigger`](crate::types::builders::ImportDataRuleBuilder::trigger)
    /// - [`size_less_than`](crate::types::builders::ImportDataRuleBuilder::size_less_than)
    pub fn build(self) -> ::std::result::Result<crate::types::ImportDataRule, ::aws_smithy_types::error::operation::BuildError> {
        ::std::result::Result::Ok(crate::types::ImportDataRule {
            prefix: self.prefix.ok_or_else(|| {
                ::aws_smithy_types::error::operation::BuildError::missing_field(
                    "prefix",
                    "prefix was not specified but it is required when building ImportDataRule",
                )
            })?,
            trigger: self.trigger.ok_or_else(|| {
                ::aws_smithy_types::error::operation::BuildError::missing_field(
                    "trigger",
                    "trigger was not specified but it is required when building ImportDataRule",
                )
            })?,
            size_less_than: self.size_less_than.ok_or_else(|| {
                ::aws_smithy_types::error::operation::BuildError::missing_field(
                    "size_less_than",
                    "size_less_than was not specified but it is required when building ImportDataRule",
                )
            })?,
        })
    }
}
