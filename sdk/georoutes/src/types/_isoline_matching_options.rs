// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>Controls how origin and destination points are matched to the road network when they don't fall exactly on a road. Matching options help ensure realistic routing by connecting points to appropriate roads.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq)]
pub struct IsolineMatchingOptions {
    /// <p>The expected street name near the point. Helps disambiguate matching when multiple roads are within range.</p>
    pub name_hint: ::std::option::Option<::std::string::String>,
    /// <p>The maximum distance in meters that a point can be from a road while still being considered "on" that road. Points further than this distance require explicit matching.</p>
    /// <p><b>Unit</b>: <code>meters</code></p>
    pub on_road_threshold: i64,
    /// <p>The maximum distance in meters to search for roads to match to. Points with no roads within this radius will fail to match. The roads that are considered within this radius are determined by the specified <code>Strategy</code></p>
    /// <p><b>Unit</b>: <code>meters</code></p>
    pub radius: i64,
    /// <p>Determines how points are matched to the road network. <code>MatchAny</code> finds the nearest viable road segment, while <code>MatchMostSignificantRoad</code> prioritizes major roads.</p>
    pub strategy: ::std::option::Option<crate::types::MatchingStrategy>,
}
impl IsolineMatchingOptions {
    /// <p>The expected street name near the point. Helps disambiguate matching when multiple roads are within range.</p>
    pub fn name_hint(&self) -> ::std::option::Option<&str> {
        self.name_hint.as_deref()
    }
    /// <p>The maximum distance in meters that a point can be from a road while still being considered "on" that road. Points further than this distance require explicit matching.</p>
    /// <p><b>Unit</b>: <code>meters</code></p>
    pub fn on_road_threshold(&self) -> i64 {
        self.on_road_threshold
    }
    /// <p>The maximum distance in meters to search for roads to match to. Points with no roads within this radius will fail to match. The roads that are considered within this radius are determined by the specified <code>Strategy</code></p>
    /// <p><b>Unit</b>: <code>meters</code></p>
    pub fn radius(&self) -> i64 {
        self.radius
    }
    /// <p>Determines how points are matched to the road network. <code>MatchAny</code> finds the nearest viable road segment, while <code>MatchMostSignificantRoad</code> prioritizes major roads.</p>
    pub fn strategy(&self) -> ::std::option::Option<&crate::types::MatchingStrategy> {
        self.strategy.as_ref()
    }
}
impl ::std::fmt::Debug for IsolineMatchingOptions {
    fn fmt(&self, f: &mut ::std::fmt::Formatter<'_>) -> ::std::fmt::Result {
        let mut formatter = f.debug_struct("IsolineMatchingOptions");
        formatter.field("name_hint", &"*** Sensitive Data Redacted ***");
        formatter.field("on_road_threshold", &"*** Sensitive Data Redacted ***");
        formatter.field("radius", &"*** Sensitive Data Redacted ***");
        formatter.field("strategy", &self.strategy);
        formatter.finish()
    }
}
impl IsolineMatchingOptions {
    /// Creates a new builder-style object to manufacture [`IsolineMatchingOptions`](crate::types::IsolineMatchingOptions).
    pub fn builder() -> crate::types::builders::IsolineMatchingOptionsBuilder {
        crate::types::builders::IsolineMatchingOptionsBuilder::default()
    }
}

/// A builder for [`IsolineMatchingOptions`](crate::types::IsolineMatchingOptions).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default)]
#[non_exhaustive]
pub struct IsolineMatchingOptionsBuilder {
    pub(crate) name_hint: ::std::option::Option<::std::string::String>,
    pub(crate) on_road_threshold: ::std::option::Option<i64>,
    pub(crate) radius: ::std::option::Option<i64>,
    pub(crate) strategy: ::std::option::Option<crate::types::MatchingStrategy>,
}
impl IsolineMatchingOptionsBuilder {
    /// <p>The expected street name near the point. Helps disambiguate matching when multiple roads are within range.</p>
    pub fn name_hint(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.name_hint = ::std::option::Option::Some(input.into());
        self
    }
    /// <p>The expected street name near the point. Helps disambiguate matching when multiple roads are within range.</p>
    pub fn set_name_hint(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.name_hint = input;
        self
    }
    /// <p>The expected street name near the point. Helps disambiguate matching when multiple roads are within range.</p>
    pub fn get_name_hint(&self) -> &::std::option::Option<::std::string::String> {
        &self.name_hint
    }
    /// <p>The maximum distance in meters that a point can be from a road while still being considered "on" that road. Points further than this distance require explicit matching.</p>
    /// <p><b>Unit</b>: <code>meters</code></p>
    pub fn on_road_threshold(mut self, input: i64) -> Self {
        self.on_road_threshold = ::std::option::Option::Some(input);
        self
    }
    /// <p>The maximum distance in meters that a point can be from a road while still being considered "on" that road. Points further than this distance require explicit matching.</p>
    /// <p><b>Unit</b>: <code>meters</code></p>
    pub fn set_on_road_threshold(mut self, input: ::std::option::Option<i64>) -> Self {
        self.on_road_threshold = input;
        self
    }
    /// <p>The maximum distance in meters that a point can be from a road while still being considered "on" that road. Points further than this distance require explicit matching.</p>
    /// <p><b>Unit</b>: <code>meters</code></p>
    pub fn get_on_road_threshold(&self) -> &::std::option::Option<i64> {
        &self.on_road_threshold
    }
    /// <p>The maximum distance in meters to search for roads to match to. Points with no roads within this radius will fail to match. The roads that are considered within this radius are determined by the specified <code>Strategy</code></p>
    /// <p><b>Unit</b>: <code>meters</code></p>
    pub fn radius(mut self, input: i64) -> Self {
        self.radius = ::std::option::Option::Some(input);
        self
    }
    /// <p>The maximum distance in meters to search for roads to match to. Points with no roads within this radius will fail to match. The roads that are considered within this radius are determined by the specified <code>Strategy</code></p>
    /// <p><b>Unit</b>: <code>meters</code></p>
    pub fn set_radius(mut self, input: ::std::option::Option<i64>) -> Self {
        self.radius = input;
        self
    }
    /// <p>The maximum distance in meters to search for roads to match to. Points with no roads within this radius will fail to match. The roads that are considered within this radius are determined by the specified <code>Strategy</code></p>
    /// <p><b>Unit</b>: <code>meters</code></p>
    pub fn get_radius(&self) -> &::std::option::Option<i64> {
        &self.radius
    }
    /// <p>Determines how points are matched to the road network. <code>MatchAny</code> finds the nearest viable road segment, while <code>MatchMostSignificantRoad</code> prioritizes major roads.</p>
    pub fn strategy(mut self, input: crate::types::MatchingStrategy) -> Self {
        self.strategy = ::std::option::Option::Some(input);
        self
    }
    /// <p>Determines how points are matched to the road network. <code>MatchAny</code> finds the nearest viable road segment, while <code>MatchMostSignificantRoad</code> prioritizes major roads.</p>
    pub fn set_strategy(mut self, input: ::std::option::Option<crate::types::MatchingStrategy>) -> Self {
        self.strategy = input;
        self
    }
    /// <p>Determines how points are matched to the road network. <code>MatchAny</code> finds the nearest viable road segment, while <code>MatchMostSignificantRoad</code> prioritizes major roads.</p>
    pub fn get_strategy(&self) -> &::std::option::Option<crate::types::MatchingStrategy> {
        &self.strategy
    }
    /// Consumes the builder and constructs a [`IsolineMatchingOptions`](crate::types::IsolineMatchingOptions).
    pub fn build(self) -> crate::types::IsolineMatchingOptions {
        crate::types::IsolineMatchingOptions {
            name_hint: self.name_hint,
            on_road_threshold: self.on_road_threshold.unwrap_or_default(),
            radius: self.radius.unwrap_or_default(),
            strategy: self.strategy,
        }
    }
}
impl ::std::fmt::Debug for IsolineMatchingOptionsBuilder {
    fn fmt(&self, f: &mut ::std::fmt::Formatter<'_>) -> ::std::fmt::Result {
        let mut formatter = f.debug_struct("IsolineMatchingOptionsBuilder");
        formatter.field("name_hint", &"*** Sensitive Data Redacted ***");
        formatter.field("on_road_threshold", &"*** Sensitive Data Redacted ***");
        formatter.field("radius", &"*** Sensitive Data Redacted ***");
        formatter.field("strategy", &self.strategy);
        formatter.finish()
    }
}
