// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>The configuration for an Amazon EC2 Auto Scaling warm pool attached to an Amazon EKS managed node group. Warm pools maintain pre-initialized EC2 instances alongside your Auto Scaling group that have already completed the bootup initialization process and can be kept in a <code>Stopped</code>, <code>Running</code>, or <code>Hibernated</code> state.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct WarmPoolConfig {
    /// <p>Specifies whether to attach warm pools on the managed node group. Set to <code>true</code> to enable the warm pool, or <code>false</code> to disable and remove it. If not specified during an update, the current value is preserved.</p>
    pub enabled: ::std::option::Option<bool>,
    /// <p>The minimum number of instances to maintain in the warm pool. Default: <code>0</code>. Size your warm pool based on scaling patterns to balance cost and availability. Start with 10-20% of expected peak capacity.</p>
    pub min_size: ::std::option::Option<i32>,
    /// <p>The maximum total number of instances across the warm pool and Auto Scaling group combined. This value controls the total prepared capacity available for your node group.</p>
    pub max_group_prepared_capacity: ::std::option::Option<i32>,
    /// <p>The desired state for warm pool instances. Default: <code>Stopped</code>. Valid values are <code>Stopped</code> (most cost-effective with EBS storage costs only), <code>Running</code> (fastest transition time with full EC2 costs), and <code>Hibernated</code> (balance between cost and speed, only supported on specific instance types). Warm pool instances in the <code>Hibernated</code> state are not supported with Bottlerocket AMIs.</p>
    pub pool_state: ::std::option::Option<crate::types::WarmPoolState>,
    /// <p>Indicates whether instances should return to the warm pool during scale-in events instead of being terminated. Default: <code>false</code>. Enable this to reduce costs by reusing instances. This feature is not supported for Bottlerocket AMIs.</p>
    pub reuse_on_scale_in: ::std::option::Option<bool>,
}
impl WarmPoolConfig {
    /// <p>Specifies whether to attach warm pools on the managed node group. Set to <code>true</code> to enable the warm pool, or <code>false</code> to disable and remove it. If not specified during an update, the current value is preserved.</p>
    pub fn enabled(&self) -> ::std::option::Option<bool> {
        self.enabled
    }
    /// <p>The minimum number of instances to maintain in the warm pool. Default: <code>0</code>. Size your warm pool based on scaling patterns to balance cost and availability. Start with 10-20% of expected peak capacity.</p>
    pub fn min_size(&self) -> ::std::option::Option<i32> {
        self.min_size
    }
    /// <p>The maximum total number of instances across the warm pool and Auto Scaling group combined. This value controls the total prepared capacity available for your node group.</p>
    pub fn max_group_prepared_capacity(&self) -> ::std::option::Option<i32> {
        self.max_group_prepared_capacity
    }
    /// <p>The desired state for warm pool instances. Default: <code>Stopped</code>. Valid values are <code>Stopped</code> (most cost-effective with EBS storage costs only), <code>Running</code> (fastest transition time with full EC2 costs), and <code>Hibernated</code> (balance between cost and speed, only supported on specific instance types). Warm pool instances in the <code>Hibernated</code> state are not supported with Bottlerocket AMIs.</p>
    pub fn pool_state(&self) -> ::std::option::Option<&crate::types::WarmPoolState> {
        self.pool_state.as_ref()
    }
    /// <p>Indicates whether instances should return to the warm pool during scale-in events instead of being terminated. Default: <code>false</code>. Enable this to reduce costs by reusing instances. This feature is not supported for Bottlerocket AMIs.</p>
    pub fn reuse_on_scale_in(&self) -> ::std::option::Option<bool> {
        self.reuse_on_scale_in
    }
}
impl WarmPoolConfig {
    /// Creates a new builder-style object to manufacture [`WarmPoolConfig`](crate::types::WarmPoolConfig).
    pub fn builder() -> crate::types::builders::WarmPoolConfigBuilder {
        crate::types::builders::WarmPoolConfigBuilder::default()
    }
}

/// A builder for [`WarmPoolConfig`](crate::types::WarmPoolConfig).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct WarmPoolConfigBuilder {
    pub(crate) enabled: ::std::option::Option<bool>,
    pub(crate) min_size: ::std::option::Option<i32>,
    pub(crate) max_group_prepared_capacity: ::std::option::Option<i32>,
    pub(crate) pool_state: ::std::option::Option<crate::types::WarmPoolState>,
    pub(crate) reuse_on_scale_in: ::std::option::Option<bool>,
}
impl WarmPoolConfigBuilder {
    /// <p>Specifies whether to attach warm pools on the managed node group. Set to <code>true</code> to enable the warm pool, or <code>false</code> to disable and remove it. If not specified during an update, the current value is preserved.</p>
    pub fn enabled(mut self, input: bool) -> Self {
        self.enabled = ::std::option::Option::Some(input);
        self
    }
    /// <p>Specifies whether to attach warm pools on the managed node group. Set to <code>true</code> to enable the warm pool, or <code>false</code> to disable and remove it. If not specified during an update, the current value is preserved.</p>
    pub fn set_enabled(mut self, input: ::std::option::Option<bool>) -> Self {
        self.enabled = input;
        self
    }
    /// <p>Specifies whether to attach warm pools on the managed node group. Set to <code>true</code> to enable the warm pool, or <code>false</code> to disable and remove it. If not specified during an update, the current value is preserved.</p>
    pub fn get_enabled(&self) -> &::std::option::Option<bool> {
        &self.enabled
    }
    /// <p>The minimum number of instances to maintain in the warm pool. Default: <code>0</code>. Size your warm pool based on scaling patterns to balance cost and availability. Start with 10-20% of expected peak capacity.</p>
    pub fn min_size(mut self, input: i32) -> Self {
        self.min_size = ::std::option::Option::Some(input);
        self
    }
    /// <p>The minimum number of instances to maintain in the warm pool. Default: <code>0</code>. Size your warm pool based on scaling patterns to balance cost and availability. Start with 10-20% of expected peak capacity.</p>
    pub fn set_min_size(mut self, input: ::std::option::Option<i32>) -> Self {
        self.min_size = input;
        self
    }
    /// <p>The minimum number of instances to maintain in the warm pool. Default: <code>0</code>. Size your warm pool based on scaling patterns to balance cost and availability. Start with 10-20% of expected peak capacity.</p>
    pub fn get_min_size(&self) -> &::std::option::Option<i32> {
        &self.min_size
    }
    /// <p>The maximum total number of instances across the warm pool and Auto Scaling group combined. This value controls the total prepared capacity available for your node group.</p>
    pub fn max_group_prepared_capacity(mut self, input: i32) -> Self {
        self.max_group_prepared_capacity = ::std::option::Option::Some(input);
        self
    }
    /// <p>The maximum total number of instances across the warm pool and Auto Scaling group combined. This value controls the total prepared capacity available for your node group.</p>
    pub fn set_max_group_prepared_capacity(mut self, input: ::std::option::Option<i32>) -> Self {
        self.max_group_prepared_capacity = input;
        self
    }
    /// <p>The maximum total number of instances across the warm pool and Auto Scaling group combined. This value controls the total prepared capacity available for your node group.</p>
    pub fn get_max_group_prepared_capacity(&self) -> &::std::option::Option<i32> {
        &self.max_group_prepared_capacity
    }
    /// <p>The desired state for warm pool instances. Default: <code>Stopped</code>. Valid values are <code>Stopped</code> (most cost-effective with EBS storage costs only), <code>Running</code> (fastest transition time with full EC2 costs), and <code>Hibernated</code> (balance between cost and speed, only supported on specific instance types). Warm pool instances in the <code>Hibernated</code> state are not supported with Bottlerocket AMIs.</p>
    pub fn pool_state(mut self, input: crate::types::WarmPoolState) -> Self {
        self.pool_state = ::std::option::Option::Some(input);
        self
    }
    /// <p>The desired state for warm pool instances. Default: <code>Stopped</code>. Valid values are <code>Stopped</code> (most cost-effective with EBS storage costs only), <code>Running</code> (fastest transition time with full EC2 costs), and <code>Hibernated</code> (balance between cost and speed, only supported on specific instance types). Warm pool instances in the <code>Hibernated</code> state are not supported with Bottlerocket AMIs.</p>
    pub fn set_pool_state(mut self, input: ::std::option::Option<crate::types::WarmPoolState>) -> Self {
        self.pool_state = input;
        self
    }
    /// <p>The desired state for warm pool instances. Default: <code>Stopped</code>. Valid values are <code>Stopped</code> (most cost-effective with EBS storage costs only), <code>Running</code> (fastest transition time with full EC2 costs), and <code>Hibernated</code> (balance between cost and speed, only supported on specific instance types). Warm pool instances in the <code>Hibernated</code> state are not supported with Bottlerocket AMIs.</p>
    pub fn get_pool_state(&self) -> &::std::option::Option<crate::types::WarmPoolState> {
        &self.pool_state
    }
    /// <p>Indicates whether instances should return to the warm pool during scale-in events instead of being terminated. Default: <code>false</code>. Enable this to reduce costs by reusing instances. This feature is not supported for Bottlerocket AMIs.</p>
    pub fn reuse_on_scale_in(mut self, input: bool) -> Self {
        self.reuse_on_scale_in = ::std::option::Option::Some(input);
        self
    }
    /// <p>Indicates whether instances should return to the warm pool during scale-in events instead of being terminated. Default: <code>false</code>. Enable this to reduce costs by reusing instances. This feature is not supported for Bottlerocket AMIs.</p>
    pub fn set_reuse_on_scale_in(mut self, input: ::std::option::Option<bool>) -> Self {
        self.reuse_on_scale_in = input;
        self
    }
    /// <p>Indicates whether instances should return to the warm pool during scale-in events instead of being terminated. Default: <code>false</code>. Enable this to reduce costs by reusing instances. This feature is not supported for Bottlerocket AMIs.</p>
    pub fn get_reuse_on_scale_in(&self) -> &::std::option::Option<bool> {
        &self.reuse_on_scale_in
    }
    /// Consumes the builder and constructs a [`WarmPoolConfig`](crate::types::WarmPoolConfig).
    pub fn build(self) -> crate::types::WarmPoolConfig {
        crate::types::WarmPoolConfig {
            enabled: self.enabled,
            min_size: self.min_size,
            max_group_prepared_capacity: self.max_group_prepared_capacity,
            pool_state: self.pool_state,
            reuse_on_scale_in: self.reuse_on_scale_in,
        }
    }
}
