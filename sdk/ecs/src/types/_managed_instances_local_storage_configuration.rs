// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>The local storage configuration for Amazon ECS Managed Instances. This defines how ECS uses and configures instance store volumes available on container instance.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct ManagedInstancesLocalStorageConfiguration {
    /// <p>Use instance store volumes for data storage when available. EBS volumes are not provisioned for data storage. If the container instance has multiple instance store volumes, a single data volume is created. Consider defining instance store requirements using the <code>localStorage</code>, <code>localStorageTypes</code> and <code>totalLocalStorageGB</code> properties.</p>
    pub use_local_storage: bool,
}
impl ManagedInstancesLocalStorageConfiguration {
    /// <p>Use instance store volumes for data storage when available. EBS volumes are not provisioned for data storage. If the container instance has multiple instance store volumes, a single data volume is created. Consider defining instance store requirements using the <code>localStorage</code>, <code>localStorageTypes</code> and <code>totalLocalStorageGB</code> properties.</p>
    pub fn use_local_storage(&self) -> bool {
        self.use_local_storage
    }
}
impl ManagedInstancesLocalStorageConfiguration {
    /// Creates a new builder-style object to manufacture [`ManagedInstancesLocalStorageConfiguration`](crate::types::ManagedInstancesLocalStorageConfiguration).
    pub fn builder() -> crate::types::builders::ManagedInstancesLocalStorageConfigurationBuilder {
        crate::types::builders::ManagedInstancesLocalStorageConfigurationBuilder::default()
    }
}

/// A builder for [`ManagedInstancesLocalStorageConfiguration`](crate::types::ManagedInstancesLocalStorageConfiguration).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct ManagedInstancesLocalStorageConfigurationBuilder {
    pub(crate) use_local_storage: ::std::option::Option<bool>,
}
impl ManagedInstancesLocalStorageConfigurationBuilder {
    /// <p>Use instance store volumes for data storage when available. EBS volumes are not provisioned for data storage. If the container instance has multiple instance store volumes, a single data volume is created. Consider defining instance store requirements using the <code>localStorage</code>, <code>localStorageTypes</code> and <code>totalLocalStorageGB</code> properties.</p>
    pub fn use_local_storage(mut self, input: bool) -> Self {
        self.use_local_storage = ::std::option::Option::Some(input);
        self
    }
    /// <p>Use instance store volumes for data storage when available. EBS volumes are not provisioned for data storage. If the container instance has multiple instance store volumes, a single data volume is created. Consider defining instance store requirements using the <code>localStorage</code>, <code>localStorageTypes</code> and <code>totalLocalStorageGB</code> properties.</p>
    pub fn set_use_local_storage(mut self, input: ::std::option::Option<bool>) -> Self {
        self.use_local_storage = input;
        self
    }
    /// <p>Use instance store volumes for data storage when available. EBS volumes are not provisioned for data storage. If the container instance has multiple instance store volumes, a single data volume is created. Consider defining instance store requirements using the <code>localStorage</code>, <code>localStorageTypes</code> and <code>totalLocalStorageGB</code> properties.</p>
    pub fn get_use_local_storage(&self) -> &::std::option::Option<bool> {
        &self.use_local_storage
    }
    /// Consumes the builder and constructs a [`ManagedInstancesLocalStorageConfiguration`](crate::types::ManagedInstancesLocalStorageConfiguration).
    pub fn build(self) -> crate::types::ManagedInstancesLocalStorageConfiguration {
        crate::types::ManagedInstancesLocalStorageConfiguration {
            use_local_storage: self.use_local_storage.unwrap_or_default(),
        }
    }
}
