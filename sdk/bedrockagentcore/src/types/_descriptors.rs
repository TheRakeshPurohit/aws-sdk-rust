// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>Contains the descriptor configuration for a registry record. Only the field that matches the record's <code>descriptorType</code> is populated.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct Descriptors {
    /// <p>The MCP (Model Context Protocol) descriptor configuration. Populated when the record's <code>descriptorType</code> is <code>MCP</code>.</p>
    pub mcp: ::std::option::Option<crate::types::McpDescriptor>,
    /// <p>The A2A (Agent-to-Agent) descriptor configuration. Populated when the record's <code>descriptorType</code> is <code>A2A</code>.</p>
    pub a2_a: ::std::option::Option<crate::types::A2aDescriptor>,
    /// <p>The custom descriptor configuration. Populated when the record's <code>descriptorType</code> is <code>CUSTOM</code>.</p>
    pub custom: ::std::option::Option<crate::types::CustomDescriptor>,
    /// <p>The agent skills descriptor configuration. Populated when the record's <code>descriptorType</code> is <code>AGENT_SKILLS</code>.</p>
    pub agent_skills: ::std::option::Option<crate::types::AgentSkillsDescriptor>,
}
impl Descriptors {
    /// <p>The MCP (Model Context Protocol) descriptor configuration. Populated when the record's <code>descriptorType</code> is <code>MCP</code>.</p>
    pub fn mcp(&self) -> ::std::option::Option<&crate::types::McpDescriptor> {
        self.mcp.as_ref()
    }
    /// <p>The A2A (Agent-to-Agent) descriptor configuration. Populated when the record's <code>descriptorType</code> is <code>A2A</code>.</p>
    pub fn a2_a(&self) -> ::std::option::Option<&crate::types::A2aDescriptor> {
        self.a2_a.as_ref()
    }
    /// <p>The custom descriptor configuration. Populated when the record's <code>descriptorType</code> is <code>CUSTOM</code>.</p>
    pub fn custom(&self) -> ::std::option::Option<&crate::types::CustomDescriptor> {
        self.custom.as_ref()
    }
    /// <p>The agent skills descriptor configuration. Populated when the record's <code>descriptorType</code> is <code>AGENT_SKILLS</code>.</p>
    pub fn agent_skills(&self) -> ::std::option::Option<&crate::types::AgentSkillsDescriptor> {
        self.agent_skills.as_ref()
    }
}
impl Descriptors {
    /// Creates a new builder-style object to manufacture [`Descriptors`](crate::types::Descriptors).
    pub fn builder() -> crate::types::builders::DescriptorsBuilder {
        crate::types::builders::DescriptorsBuilder::default()
    }
}

/// A builder for [`Descriptors`](crate::types::Descriptors).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct DescriptorsBuilder {
    pub(crate) mcp: ::std::option::Option<crate::types::McpDescriptor>,
    pub(crate) a2_a: ::std::option::Option<crate::types::A2aDescriptor>,
    pub(crate) custom: ::std::option::Option<crate::types::CustomDescriptor>,
    pub(crate) agent_skills: ::std::option::Option<crate::types::AgentSkillsDescriptor>,
}
impl DescriptorsBuilder {
    /// <p>The MCP (Model Context Protocol) descriptor configuration. Populated when the record's <code>descriptorType</code> is <code>MCP</code>.</p>
    pub fn mcp(mut self, input: crate::types::McpDescriptor) -> Self {
        self.mcp = ::std::option::Option::Some(input);
        self
    }
    /// <p>The MCP (Model Context Protocol) descriptor configuration. Populated when the record's <code>descriptorType</code> is <code>MCP</code>.</p>
    pub fn set_mcp(mut self, input: ::std::option::Option<crate::types::McpDescriptor>) -> Self {
        self.mcp = input;
        self
    }
    /// <p>The MCP (Model Context Protocol) descriptor configuration. Populated when the record's <code>descriptorType</code> is <code>MCP</code>.</p>
    pub fn get_mcp(&self) -> &::std::option::Option<crate::types::McpDescriptor> {
        &self.mcp
    }
    /// <p>The A2A (Agent-to-Agent) descriptor configuration. Populated when the record's <code>descriptorType</code> is <code>A2A</code>.</p>
    pub fn a2_a(mut self, input: crate::types::A2aDescriptor) -> Self {
        self.a2_a = ::std::option::Option::Some(input);
        self
    }
    /// <p>The A2A (Agent-to-Agent) descriptor configuration. Populated when the record's <code>descriptorType</code> is <code>A2A</code>.</p>
    pub fn set_a2_a(mut self, input: ::std::option::Option<crate::types::A2aDescriptor>) -> Self {
        self.a2_a = input;
        self
    }
    /// <p>The A2A (Agent-to-Agent) descriptor configuration. Populated when the record's <code>descriptorType</code> is <code>A2A</code>.</p>
    pub fn get_a2_a(&self) -> &::std::option::Option<crate::types::A2aDescriptor> {
        &self.a2_a
    }
    /// <p>The custom descriptor configuration. Populated when the record's <code>descriptorType</code> is <code>CUSTOM</code>.</p>
    pub fn custom(mut self, input: crate::types::CustomDescriptor) -> Self {
        self.custom = ::std::option::Option::Some(input);
        self
    }
    /// <p>The custom descriptor configuration. Populated when the record's <code>descriptorType</code> is <code>CUSTOM</code>.</p>
    pub fn set_custom(mut self, input: ::std::option::Option<crate::types::CustomDescriptor>) -> Self {
        self.custom = input;
        self
    }
    /// <p>The custom descriptor configuration. Populated when the record's <code>descriptorType</code> is <code>CUSTOM</code>.</p>
    pub fn get_custom(&self) -> &::std::option::Option<crate::types::CustomDescriptor> {
        &self.custom
    }
    /// <p>The agent skills descriptor configuration. Populated when the record's <code>descriptorType</code> is <code>AGENT_SKILLS</code>.</p>
    pub fn agent_skills(mut self, input: crate::types::AgentSkillsDescriptor) -> Self {
        self.agent_skills = ::std::option::Option::Some(input);
        self
    }
    /// <p>The agent skills descriptor configuration. Populated when the record's <code>descriptorType</code> is <code>AGENT_SKILLS</code>.</p>
    pub fn set_agent_skills(mut self, input: ::std::option::Option<crate::types::AgentSkillsDescriptor>) -> Self {
        self.agent_skills = input;
        self
    }
    /// <p>The agent skills descriptor configuration. Populated when the record's <code>descriptorType</code> is <code>AGENT_SKILLS</code>.</p>
    pub fn get_agent_skills(&self) -> &::std::option::Option<crate::types::AgentSkillsDescriptor> {
        &self.agent_skills
    }
    /// Consumes the builder and constructs a [`Descriptors`](crate::types::Descriptors).
    pub fn build(self) -> crate::types::Descriptors {
        crate::types::Descriptors {
            mcp: self.mcp,
            a2_a: self.a2_a,
            custom: self.custom,
            agent_skills: self.agent_skills,
        }
    }
}
