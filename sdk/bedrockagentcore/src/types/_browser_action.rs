// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>The browser action to perform. Exactly one member must be set per request.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub enum BrowserAction {
    /// <p>Press a key one or more times.</p>
    KeyPress(crate::types::KeyPressArguments),
    /// <p>Press a key combination.</p>
    KeyShortcut(crate::types::KeyShortcutArguments),
    /// <p>Type a string of text.</p>
    KeyType(crate::types::KeyTypeArguments),
    /// <p>Click at the specified coordinates.</p>
    MouseClick(crate::types::MouseClickArguments),
    /// <p>Drag from a start position to an end position.</p>
    MouseDrag(crate::types::MouseDragArguments),
    /// <p>Move the cursor to the specified coordinates.</p>
    MouseMove(crate::types::MouseMoveArguments),
    /// <p>Scroll at the specified position.</p>
    MouseScroll(crate::types::MouseScrollArguments),
    /// <p>Capture a full-screen screenshot.</p>
    Screenshot(crate::types::ScreenshotArguments),
    /// The `Unknown` variant represents cases where new union variant was received. Consider upgrading the SDK to the latest available version.
    /// An unknown enum variant
    ///
    /// _Note: If you encounter this error, consider upgrading your SDK to the latest version._
    /// The `Unknown` variant represents cases where the server sent a value that wasn't recognized
    /// by the client. This can happen when the server adds new functionality, but the client has not been updated.
    /// To investigate this, consider turning on debug logging to print the raw HTTP response.
    #[non_exhaustive]
    Unknown,
}
impl BrowserAction {
    /// Tries to convert the enum instance into [`KeyPress`](crate::types::BrowserAction::KeyPress), extracting the inner [`KeyPressArguments`](crate::types::KeyPressArguments).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_key_press(&self) -> ::std::result::Result<&crate::types::KeyPressArguments, &Self> {
        if let BrowserAction::KeyPress(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`KeyPress`](crate::types::BrowserAction::KeyPress).
    pub fn is_key_press(&self) -> bool {
        self.as_key_press().is_ok()
    }
    /// Tries to convert the enum instance into [`KeyShortcut`](crate::types::BrowserAction::KeyShortcut), extracting the inner [`KeyShortcutArguments`](crate::types::KeyShortcutArguments).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_key_shortcut(&self) -> ::std::result::Result<&crate::types::KeyShortcutArguments, &Self> {
        if let BrowserAction::KeyShortcut(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`KeyShortcut`](crate::types::BrowserAction::KeyShortcut).
    pub fn is_key_shortcut(&self) -> bool {
        self.as_key_shortcut().is_ok()
    }
    /// Tries to convert the enum instance into [`KeyType`](crate::types::BrowserAction::KeyType), extracting the inner [`KeyTypeArguments`](crate::types::KeyTypeArguments).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_key_type(&self) -> ::std::result::Result<&crate::types::KeyTypeArguments, &Self> {
        if let BrowserAction::KeyType(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`KeyType`](crate::types::BrowserAction::KeyType).
    pub fn is_key_type(&self) -> bool {
        self.as_key_type().is_ok()
    }
    /// Tries to convert the enum instance into [`MouseClick`](crate::types::BrowserAction::MouseClick), extracting the inner [`MouseClickArguments`](crate::types::MouseClickArguments).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_mouse_click(&self) -> ::std::result::Result<&crate::types::MouseClickArguments, &Self> {
        if let BrowserAction::MouseClick(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`MouseClick`](crate::types::BrowserAction::MouseClick).
    pub fn is_mouse_click(&self) -> bool {
        self.as_mouse_click().is_ok()
    }
    /// Tries to convert the enum instance into [`MouseDrag`](crate::types::BrowserAction::MouseDrag), extracting the inner [`MouseDragArguments`](crate::types::MouseDragArguments).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_mouse_drag(&self) -> ::std::result::Result<&crate::types::MouseDragArguments, &Self> {
        if let BrowserAction::MouseDrag(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`MouseDrag`](crate::types::BrowserAction::MouseDrag).
    pub fn is_mouse_drag(&self) -> bool {
        self.as_mouse_drag().is_ok()
    }
    /// Tries to convert the enum instance into [`MouseMove`](crate::types::BrowserAction::MouseMove), extracting the inner [`MouseMoveArguments`](crate::types::MouseMoveArguments).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_mouse_move(&self) -> ::std::result::Result<&crate::types::MouseMoveArguments, &Self> {
        if let BrowserAction::MouseMove(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`MouseMove`](crate::types::BrowserAction::MouseMove).
    pub fn is_mouse_move(&self) -> bool {
        self.as_mouse_move().is_ok()
    }
    /// Tries to convert the enum instance into [`MouseScroll`](crate::types::BrowserAction::MouseScroll), extracting the inner [`MouseScrollArguments`](crate::types::MouseScrollArguments).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_mouse_scroll(&self) -> ::std::result::Result<&crate::types::MouseScrollArguments, &Self> {
        if let BrowserAction::MouseScroll(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`MouseScroll`](crate::types::BrowserAction::MouseScroll).
    pub fn is_mouse_scroll(&self) -> bool {
        self.as_mouse_scroll().is_ok()
    }
    /// Tries to convert the enum instance into [`Screenshot`](crate::types::BrowserAction::Screenshot), extracting the inner [`ScreenshotArguments`](crate::types::ScreenshotArguments).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_screenshot(&self) -> ::std::result::Result<&crate::types::ScreenshotArguments, &Self> {
        if let BrowserAction::Screenshot(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`Screenshot`](crate::types::BrowserAction::Screenshot).
    pub fn is_screenshot(&self) -> bool {
        self.as_screenshot().is_ok()
    }
    /// Returns true if the enum instance is the `Unknown` variant.
    pub fn is_unknown(&self) -> bool {
        matches!(self, Self::Unknown)
    }
}
