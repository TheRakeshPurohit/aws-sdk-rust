// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>Describes an ASN.1 X.400 <code>GeneralName</code> as defined in <a href="https://datatracker.ietf.org/doc/html/rfc5280">RFC 5280</a>. Only one of the following naming options should be provided.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub enum GeneralName {
    /// <p>Contains information about the certificate subject. The <code>Subject</code> field in the certificate identifies the entity that owns or controls the public key in the certificate. The entity can be a user, computer, device, or service. The <code>Subject</code> must contain an X.500 distinguished name (DN). A DN is a sequence of relative distinguished names (RDNs). The RDNs are separated by commas in the certificate.</p>
    DirectoryName(crate::types::DistinguishedName),
    /// <p>Represents <code>GeneralName</code> as a DNS name.</p>
    DnsName(::std::string::String),
    /// <p>Represents <code>GeneralName</code> as an IPv4 or IPv6 address.</p>
    IpAddress(::std::string::String),
    /// <p>Represents <code>GeneralName</code> using an <code>OtherName</code> object.</p>
    OtherName(crate::types::OtherName),
    /// <p>Represents <code>GeneralName</code> as an object identifier (OID).</p>
    RegisteredId(::std::string::String),
    /// <p>Represents <code>GeneralName</code> as an <a href="https://datatracker.ietf.org/doc/html/rfc822">RFC 822</a> email address.</p>
    Rfc822Name(::std::string::String),
    /// <p>Represents <code>GeneralName</code> as a URI.</p>
    UniformResourceIdentifier(::std::string::String),
    /// The `Unknown` variant represents cases where new union variant was received. Consider upgrading the SDK to the latest available version.
    /// An unknown enum variant
    ///
    /// _Note: If you encounter this error, consider upgrading your SDK to the latest version._
    /// The `Unknown` variant represents cases where the server sent a value that wasn't recognized
    /// by the client. This can happen when the server adds new functionality, but the client has not been updated.
    /// To investigate this, consider turning on debug logging to print the raw HTTP response.
    #[non_exhaustive]
    Unknown,
}
impl GeneralName {
    /// Tries to convert the enum instance into [`DirectoryName`](crate::types::GeneralName::DirectoryName), extracting the inner [`DistinguishedName`](crate::types::DistinguishedName).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_directory_name(&self) -> ::std::result::Result<&crate::types::DistinguishedName, &Self> {
        if let GeneralName::DirectoryName(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`DirectoryName`](crate::types::GeneralName::DirectoryName).
    pub fn is_directory_name(&self) -> bool {
        self.as_directory_name().is_ok()
    }
    /// Tries to convert the enum instance into [`DnsName`](crate::types::GeneralName::DnsName), extracting the inner [`String`](::std::string::String).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_dns_name(&self) -> ::std::result::Result<&::std::string::String, &Self> {
        if let GeneralName::DnsName(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`DnsName`](crate::types::GeneralName::DnsName).
    pub fn is_dns_name(&self) -> bool {
        self.as_dns_name().is_ok()
    }
    /// Tries to convert the enum instance into [`IpAddress`](crate::types::GeneralName::IpAddress), extracting the inner [`String`](::std::string::String).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_ip_address(&self) -> ::std::result::Result<&::std::string::String, &Self> {
        if let GeneralName::IpAddress(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`IpAddress`](crate::types::GeneralName::IpAddress).
    pub fn is_ip_address(&self) -> bool {
        self.as_ip_address().is_ok()
    }
    /// Tries to convert the enum instance into [`OtherName`](crate::types::GeneralName::OtherName), extracting the inner [`OtherName`](crate::types::OtherName).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_other_name(&self) -> ::std::result::Result<&crate::types::OtherName, &Self> {
        if let GeneralName::OtherName(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`OtherName`](crate::types::GeneralName::OtherName).
    pub fn is_other_name(&self) -> bool {
        self.as_other_name().is_ok()
    }
    /// Tries to convert the enum instance into [`RegisteredId`](crate::types::GeneralName::RegisteredId), extracting the inner [`String`](::std::string::String).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_registered_id(&self) -> ::std::result::Result<&::std::string::String, &Self> {
        if let GeneralName::RegisteredId(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`RegisteredId`](crate::types::GeneralName::RegisteredId).
    pub fn is_registered_id(&self) -> bool {
        self.as_registered_id().is_ok()
    }
    /// Tries to convert the enum instance into [`Rfc822Name`](crate::types::GeneralName::Rfc822Name), extracting the inner [`String`](::std::string::String).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_rfc822_name(&self) -> ::std::result::Result<&::std::string::String, &Self> {
        if let GeneralName::Rfc822Name(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`Rfc822Name`](crate::types::GeneralName::Rfc822Name).
    pub fn is_rfc822_name(&self) -> bool {
        self.as_rfc822_name().is_ok()
    }
    /// Tries to convert the enum instance into [`UniformResourceIdentifier`](crate::types::GeneralName::UniformResourceIdentifier), extracting the inner [`String`](::std::string::String).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_uniform_resource_identifier(&self) -> ::std::result::Result<&::std::string::String, &Self> {
        if let GeneralName::UniformResourceIdentifier(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`UniformResourceIdentifier`](crate::types::GeneralName::UniformResourceIdentifier).
    pub fn is_uniform_resource_identifier(&self) -> bool {
        self.as_uniform_resource_identifier().is_ok()
    }
    /// Returns true if the enum instance is the `Unknown` variant.
    pub fn is_unknown(&self) -> bool {
        matches!(self, Self::Unknown)
    }
}
