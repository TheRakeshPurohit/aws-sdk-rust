// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>Filters certificates by X.509 attributes.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub enum X509AttributeFilter {
    /// <p>Filter by extended key usage.</p>
    ExtendedKeyUsage(crate::types::ExtendedKeyUsageName),
    /// <p>Filter by key algorithm.</p>
    KeyAlgorithm(crate::types::KeyAlgorithm),
    /// <p>Filter by key usage.</p>
    KeyUsage(crate::types::KeyUsageName),
    /// <p>Filter by certificate expiration date. The start date is inclusive.</p>
    NotAfter(crate::types::TimestampRange),
    /// <p>Filter by certificate validity start date. The start date is inclusive.</p>
    NotBefore(crate::types::TimestampRange),
    /// <p>Filter by serial number.</p>
    SerialNumber(::std::string::String),
    /// <p>Filter by certificate subject.</p>
    Subject(crate::types::SubjectFilter),
    /// <p>Filter by subject alternative names.</p>
    SubjectAlternativeName(crate::types::SubjectAlternativeNameFilter),
    /// The `Unknown` variant represents cases where new union variant was received. Consider upgrading the SDK to the latest available version.
    /// An unknown enum variant
    ///
    /// _Note: If you encounter this error, consider upgrading your SDK to the latest version._
    /// The `Unknown` variant represents cases where the server sent a value that wasn't recognized
    /// by the client. This can happen when the server adds new functionality, but the client has not been updated.
    /// To investigate this, consider turning on debug logging to print the raw HTTP response.
    #[non_exhaustive]
    Unknown,
}
impl X509AttributeFilter {
    /// Tries to convert the enum instance into [`ExtendedKeyUsage`](crate::types::X509AttributeFilter::ExtendedKeyUsage), extracting the inner [`ExtendedKeyUsageName`](crate::types::ExtendedKeyUsageName).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_extended_key_usage(&self) -> ::std::result::Result<&crate::types::ExtendedKeyUsageName, &Self> {
        if let X509AttributeFilter::ExtendedKeyUsage(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`ExtendedKeyUsage`](crate::types::X509AttributeFilter::ExtendedKeyUsage).
    pub fn is_extended_key_usage(&self) -> bool {
        self.as_extended_key_usage().is_ok()
    }
    /// Tries to convert the enum instance into [`KeyAlgorithm`](crate::types::X509AttributeFilter::KeyAlgorithm), extracting the inner [`KeyAlgorithm`](crate::types::KeyAlgorithm).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_key_algorithm(&self) -> ::std::result::Result<&crate::types::KeyAlgorithm, &Self> {
        if let X509AttributeFilter::KeyAlgorithm(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`KeyAlgorithm`](crate::types::X509AttributeFilter::KeyAlgorithm).
    pub fn is_key_algorithm(&self) -> bool {
        self.as_key_algorithm().is_ok()
    }
    /// Tries to convert the enum instance into [`KeyUsage`](crate::types::X509AttributeFilter::KeyUsage), extracting the inner [`KeyUsageName`](crate::types::KeyUsageName).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_key_usage(&self) -> ::std::result::Result<&crate::types::KeyUsageName, &Self> {
        if let X509AttributeFilter::KeyUsage(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`KeyUsage`](crate::types::X509AttributeFilter::KeyUsage).
    pub fn is_key_usage(&self) -> bool {
        self.as_key_usage().is_ok()
    }
    /// Tries to convert the enum instance into [`NotAfter`](crate::types::X509AttributeFilter::NotAfter), extracting the inner [`TimestampRange`](crate::types::TimestampRange).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_not_after(&self) -> ::std::result::Result<&crate::types::TimestampRange, &Self> {
        if let X509AttributeFilter::NotAfter(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`NotAfter`](crate::types::X509AttributeFilter::NotAfter).
    pub fn is_not_after(&self) -> bool {
        self.as_not_after().is_ok()
    }
    /// Tries to convert the enum instance into [`NotBefore`](crate::types::X509AttributeFilter::NotBefore), extracting the inner [`TimestampRange`](crate::types::TimestampRange).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_not_before(&self) -> ::std::result::Result<&crate::types::TimestampRange, &Self> {
        if let X509AttributeFilter::NotBefore(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`NotBefore`](crate::types::X509AttributeFilter::NotBefore).
    pub fn is_not_before(&self) -> bool {
        self.as_not_before().is_ok()
    }
    /// Tries to convert the enum instance into [`SerialNumber`](crate::types::X509AttributeFilter::SerialNumber), extracting the inner [`String`](::std::string::String).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_serial_number(&self) -> ::std::result::Result<&::std::string::String, &Self> {
        if let X509AttributeFilter::SerialNumber(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`SerialNumber`](crate::types::X509AttributeFilter::SerialNumber).
    pub fn is_serial_number(&self) -> bool {
        self.as_serial_number().is_ok()
    }
    /// Tries to convert the enum instance into [`Subject`](crate::types::X509AttributeFilter::Subject), extracting the inner [`SubjectFilter`](crate::types::SubjectFilter).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_subject(&self) -> ::std::result::Result<&crate::types::SubjectFilter, &Self> {
        if let X509AttributeFilter::Subject(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`Subject`](crate::types::X509AttributeFilter::Subject).
    pub fn is_subject(&self) -> bool {
        self.as_subject().is_ok()
    }
    /// Tries to convert the enum instance into [`SubjectAlternativeName`](crate::types::X509AttributeFilter::SubjectAlternativeName), extracting the inner [`SubjectAlternativeNameFilter`](crate::types::SubjectAlternativeNameFilter).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_subject_alternative_name(&self) -> ::std::result::Result<&crate::types::SubjectAlternativeNameFilter, &Self> {
        if let X509AttributeFilter::SubjectAlternativeName(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`SubjectAlternativeName`](crate::types::X509AttributeFilter::SubjectAlternativeName).
    pub fn is_subject_alternative_name(&self) -> bool {
        self.as_subject_alternative_name().is_ok()
    }
    /// Returns true if the enum instance is the `Unknown` variant.
    pub fn is_unknown(&self) -> bool {
        matches!(self, Self::Unknown)
    }
}
